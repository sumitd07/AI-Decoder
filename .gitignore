# Dependencies
node_modules/

# Env & secrets
.env
.env.*
!.env.example

# Build / deploy
dist/
.vercel/
.netlify/

# Eval harness output (eval/run.js) — regenerated per run, per shelf version
eval/out/

# OS / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Local notes (keep strategy docs, ignore scratch)
scratch/

# Extension store packages (build artifacts)
*.zip
